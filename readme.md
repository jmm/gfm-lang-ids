Generate markdown document listing valid language identifier keywords for enabling syntax highlighting in GitHub Flavored Markdown.

```
$ npm install
$ node . --linguist-version=VERSION --lang-yml-path=PATH/TO/languages.yml
```
